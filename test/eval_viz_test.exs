defmodule EvalVizTest do
  use ExUnit.Case, async: true

  # The examples in @doc are the ones people copy first, so they run here.
  # An earlier example called VegaLite.title/2, which does not exist; nothing
  # caught it because nothing executed the docs.
  doctest EvalViz
end
